name: Publish podcast

on:
  push:
    branches: [main]
    paths:
      - "Insights/**"
      - "Podcast/Episodes/**"
      - "Podcast/Assets/**"
      - "Podcast/podcast.yml"
      - "Tools/**"
      - "tests/**"
      - ".github/workflows/publish-podcast.yml"
  workflow_dispatch:
    inputs:
      generation_only:
        description: Generate one podcast artifact without publishing it
        required: true
        default: false
        type: boolean
      source_path:
        description: Repository-relative Markdown path used when generation_only is enabled
        required: false
        default: ""
        type: string
      duration_minutes:
        description: Target duration used when generation_only is enabled
        required: false
        default: "10"
        type: string
      force_regeneration:
        description: Regenerate all published episodes even if source is unchanged
        required: true
        default: false
        type: boolean

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: podcast-publishing
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      PODCAST_EMAIL: ${{ vars.PODCAST_EMAIL }}
      YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
      YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
      YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
      YOUTUBE_PRIVACY_STATUS: ${{ vars.YOUTUBE_PRIVACY_STATUS || 'private' }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Tools/requirements.txt

      - name: Install Python dependencies
        run: python -m pip install -r Tools/requirements.txt

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install --yes ffmpeg

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Generate one unpublished podcast artifact
        if: ${{ inputs.generation_only }}
        env:
          SOURCE_PATH: ${{ inputs.source_path }}
          DURATION_MINUTES: ${{ inputs.duration_minutes }}
          OBSIDIAN_VAULT_PATH: ${{ github.workspace }}
          INSIGHTS_DIRECTORY: Insights
        run: |
          case "$SOURCE_PATH" in
            Insights/*.md) ;;
            *) echo "source_path must match Insights/*.md" >&2; exit 2 ;;
          esac
          python Tools/generate_podcast.py "$SOURCE_PATH" --duration "$DURATION_MINUTES"

      - name: Build changed episodes
        if: ${{ !inputs.generation_only && !inputs.force_regeneration }}
        run: python Tools/run_pipeline.py --strict-email

      - name: Force-build all episodes
        if: ${{ !inputs.generation_only && inputs.force_regeneration }}
        run: python Tools/run_pipeline.py --strict-email --force

      - name: Publish missing episodes to YouTube
        if: ${{ !inputs.generation_only }}
        run: python Tools/youtube_publish.py

      - name: Commit durable episode outputs
        if: ${{ !inputs.generation_only }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Podcast/Scripts Podcast/Audio Podcast/Metadata
          if git diff --cached --quiet; then
            echo "No durable episode changes"
          else
            git commit -m "Publish generated podcast episode assets [skip ci]"
            git push
          fi

      - name: Upload downloadable podcast artifact
        uses: actions/upload-artifact@v7
        with:
          name: podcast-${{ github.run_number }}
          path: |
            Podcast/Scripts/*.md
            Podcast/Audio/*.mp3
            Podcast/Metadata/*.json
            Podcast/Public/podcast.xml
          if-no-files-found: error
          retention-days: 30

      - name: Configure GitHub Pages
        if: ${{ !inputs.generation_only }}
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        if: ${{ !inputs.generation_only }}
        uses: actions/upload-pages-artifact@v4
        with:
          path: Podcast/Public

  deploy:
    if: ${{ !inputs.generation_only }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
